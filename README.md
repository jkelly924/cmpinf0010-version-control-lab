# This is a simple program that takes the user's `name`, and an integer `rotation_amount`. It then outputs the name rotated to the right by that given amount.

# Credits:
- Jayden Kelly
- Cole DeSimone
